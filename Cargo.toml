[package]
name = "krusty"
version = "0.1.0"
edition = "2021"

[profile.dev]
opt-level = 1
[profile.dev.package."*"]
opt-level = 3

[dependencies]
bevy = { version = "0.7.0", features = ["dynamic"] }
bevy_svg = { version = "0.7", default-features = false, features = ["2d" ]}
rand = "0.8.5"

